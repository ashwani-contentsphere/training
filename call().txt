var circle = {};

circle.radius = 5;
circle.area = function(){
                return this.radius * this. radius * Math.PI;
                };

console.log("area is => " + circle.area());


var fn = circle.area;
fn.radius=10;

console.log("area using call => " + fn.call(fn));
